package main;

import static org.junit.Assert.*;
import java.util.ArrayList;

import org.junit.Test;

import main.BinarySearchTree.Leaf;

public class BinarySearchTreeTest {

	//checkBstness determines that all left children have a lesser value than it's parent
	// and that all right children are greater than or equal to it's value. The public function
	// calls the private function of the same name on the root notes of a BinarySearchTree
	@Test
	public void checkBstness(BinarySearchTree test) {

    }

    //addTest checks to see that the add function inserts new leaves into a BinarySearchTree in the
    //appropriate location regardless of the order inserteds
	@Test
	public void addTest() {
	}
}
